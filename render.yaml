services:
  - type: web
    name: nigerian-tax-compliance-backend
    env: python
    plan: free
    runtime: python-3.11.8
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
